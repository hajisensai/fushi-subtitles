name: fushi_asr_align
description: EPUB and text to audio alignment - sentence matching, anchor gap filling, cue resegmentation.
version: 0.1.1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  fushi_asr_core: ^0.1.0
  meta: ^1.15.0
  archive: ^4.0.0
  html: ^0.15.0
  xml: ^6.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
