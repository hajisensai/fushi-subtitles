name: fushi_asr_cli
description: fushi-subs command line - transcribe, models, serve.
version: 0.1.1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  args: ^2.6.0
  fushi_asr: ^0.1.0
  fushi_asr_server: ^0.1.0

dev_dependencies:
  archive: ^4.0.0
  lints: ^5.0.0
  test: ^1.25.0

executables:
  fushi-subs: asr
